sudo: required
services:
    - docker

before_install:
    - docker build -t stevanovicmarko/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true stevanovicmarko/docker-react npm run test -- --coverage